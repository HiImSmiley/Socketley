# Socketley
Socketley is a Linux‑only daemon and CLI tool that manages long‑living network runtimes (servers, clients, proxies, caches) with a strong focus on performance, determinism, and explicit lifecycle control.
